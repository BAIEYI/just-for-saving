@1@ YAML（Yet Another Markup Language）
是一种直观的数据序列化格式，用于配置文件、数据交换、数据存储等。
它是一种人类可读的格式，使用空白字符和缩进来表示数据层次结构，而不是使用括号或标点符号。
YAML的目的是为了提供一种比XML或JSON更为简洁和易于理解的配置文件格式。
YAML文件通常具有.yaml或.yml的文件扩展名

@2@ PT文件
在PyTorch深度学习框架中，PT文件（通常以.pt或.pth为扩展名）是用于保存和加载模型权重及结构的一种二进制格式。
开发者可以将训练好的模型（包括其结构和权重）保存为PT文件。
这玩意至今为止2024.9.13，我每次与其打交道的经历只能用悲剧来形容~~~~

@3@ 代理服务器
位置：代理服务器位于客户端（例如用户的计算机或设备）和目标服务器（例如网站或网络服务）之间。
功能：当客户端想要访问网络资源时，它不是直接与目标服务器通信，而是将请求发送到代理服务器。代理服务器然后代表客户端转发请求到目标服务器，并将从目标服务器收到的响应返回给客户端。
作用上通俗的来理解的话，代理服务器既可以理解成一个审核，看看客户和目标服务器之间都交流些什么，并从中拦截（大概也是可以篡改）些什么吧；又可以理解成盾牌，关键时候挡一手~

@4@ 关于matplotlib库如何使用
https://www.runoob.com/matplotlib/matplotlib-marker.html
终于有个地方把它给讲明白咯，这个可以用作查询&学习